import React from "react";
import ReactDOM from "react-dom";
import { App } from "./App";

ReactDOM.render(<App />, document.querySelector("#root"));

// Te container does not need a mount function like the remote apps
